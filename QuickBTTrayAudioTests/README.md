# QuickBTTrayAudioTests

Dependency-free native behavior test for the Phase 2 APO DLL. It is built separately from the managed application with the x64 Release MSBuild command documented in `QuickBTTrayAudio/README.md`.
